export enum Routes {
  Loader = '_loader',
  Splash = '_splash',
  OnBoarding = '_onboadring',
  MainApp = '_main_app',
  Login = '_login',
  Register = '_register',
  Home = '_home',
  Default = '_default',
  DefaultB = '_defaultb',
  DefaultC = '_defaultc',
  Profile = '_profile',
}
